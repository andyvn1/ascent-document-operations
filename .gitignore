# Local-only AI assistant context — never tracked
AI.md

# Local-only project config — never tracked
config/

# Business/pricing docs and learning/master-plan docs — never tracked
# (repo is public; ADRs and architecture docs are public, this stays
# private since it's competitive/pricing research, not architecture)
docs/business/
docs/learning/

# Environment / secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node (web app)
node_modules/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Real customer documents (only test fixtures may be committed)
*.pdf
!tests/fixtures/**/*.pdf

# Local file storage backend — uploaded file content, not source
storage/
