services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=development
      - LOG_LEVEL=DEBUG
    volumes:
      - .:/app
      - /app/.venv
    command: uv run uvicorn apps.api.main:app --host 0.0.0.0 --port 8000 --reload